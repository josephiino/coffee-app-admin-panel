# apphosting.yaml
runConfig:
  minInstances: 0 # trafiksizken sıfıra iner → maliyet yok
  maxInstances: 50
  concurrency: 80 # isteği aynı anda işler

scripts:
  runCommand: node server.js # “npm start” yerine

